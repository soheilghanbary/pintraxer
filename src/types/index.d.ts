type TParams = {
  params: {
    id: string
  }
}

type TUser = {
  id: string
  email: string
  image: string
  name: string
  bio?: string
  website?: string
}

type Board = {
  // Define the structure of the Board type here
}

type Pin = {
  // Define the structure of the Pin type here
}
